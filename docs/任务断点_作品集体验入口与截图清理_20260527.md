# 任务断点：作品集体验入口与截图清理

**日期：** 2026-05-27
**状态：** 暂停，待恢复

---

## 一、当前任务目标

在作品集网站中新增"项目体验入口"功能（`/try` 汇总页、ProdDoc AI Demo、Decision Copilot Demo），修复 heard-sheep 真实体验链接 404 问题，并清理 heard-sheep 产品截图中不应出现的覆盖元素（右侧粉色悬浮按钮、左下角黑色 `N` 开发标记）。

---

## 二、今天已经完成的事项

### 首页体验入口

- [x] 首页已新增"体验"导航项（Header: 作品 / 体验 / 听到了咩 / ProdDoc AI / GitHub）
- [x] 首页各项目已新增体验按钮：
  - 听到了咩：`立即体验` + `查看案例`
  - ProdDoc AI：`体验 Demo` + `查看案例`
  - Decision Copilot：`体验实验` + `详情`

### /try 页面

- [x] `/try` 汇总页已创建（`src/app/try/page.tsx`）
- [x] `/try/proddoc-ai` Demo 已创建（`src/app/try/proddoc-ai/page.tsx`，Client Component）
- [x] `/try/decision-copilot` Demo 已创建（`src/app/try/decision-copilot/page.tsx`，Client Component）
- [x] Build 验证通过，11 个静态页面全部生成

### heard-sheep 体验链接修复

- [x] `src/data/site-config.ts` 中 `heardSheepLiveUrl` 已调整为环境感知配置：
  - 开发环境：`http://localhost:3001/sheep`
  - 生产环境：`/sheep`
  - 支持 `NEXT_PUBLIC_HEARD_SHEEP_URL` 环境变量覆盖
- [x] `heard-sheep` 详情页已新增"立即体验"按钮

### 配置与数据文件

- [x] `src/data/projects.ts` 接口新增字段：`tryPath`、`tryLabel`、`experienceTag`、`experienceDescription`、`displayMode`、`homepageSummary`、`homepageImages`
- [x] `src/data/demos/proddoc-ai.ts` 已创建（表单默认值、选项、模板生成函数）
- [x] `src/data/demos/decision-copilot.ts` 已创建（3 个预设决策案例）
- [x] `docs/如何手动修改作品集网站内容.md` 已更新（新增第 10-15 节）

### 截图（部分更新）

- [x] `public/projects/heard-sheep/home-mobile.png` — 已重截为干净版本（23:32）
- [x] `public/projects/heard-sheep/analysis-result-mobile.png` — 已重截为干净版本（23:34）
- [x] `public/projects/heard-sheep/tasks-with-data-mobile.png` — 已重截为干净版本（23:34）
- [x] `public/projects/heard-sheep/history-mobile.png` — 已重截为干净版本（23:35）
- [x] `public/projects/heard-sheep/me-mobile.png` — 已重截为干净版本（23:41）

### 验证

- [x] TypeScript (`tsc --noEmit`) 通过
- [x] `npm run build` 通过（11 页面）
- [x] 占位信息扫描通过
- [x] 明文密钥扫描通过

---

## 三、今天尚未完成的事项

### 截图污染问题

- [ ] `public/projects/heard-sheep/input-mobile.png` — 仍为旧版（22:09），可能含覆盖元素
- [ ] `public/projects/heard-sheep/candidate-tasks-mobile.png` — 仍为旧版（22:10），可能含覆盖元素
- [ ] `public/projects/heard-sheep/task-detail-mobile.png` — 仍为旧版（22:11），可能含覆盖元素
- [ ] `public/projects/heard-sheep/tasks-mobile.png` — 仍为旧版（22:08），空状态页面

### 截图环境阻塞

- 为截图启动的 Chrome 调试端口（9222）不稳定，多次断连
- 已使用 `--user-data-dir` 临时配置目录解决实例冲突
- Chrome 进程已清理，调试端口已停止

### 最终复核

- [ ] 尚未完成新的 TypeScript / build / 占位信息 / 敏感信息最终复核（因截图未全部完成）
- [ ] 尚未执行 Figma Code to Canvas 最终捕获
- [ ] 尚未执行 Git 提交、GitHub 推送和域名部署

---

## 四、当前风险和注意事项

1. **截图质量：** 不得再使用含粉色悬浮控件或黑色 `N` 标记的图片作为最终作品集素材
2. **本地服务：** 作品集运行于 `http://localhost:3000`，heard-sheep 独立产品本地体验应指向 `http://localhost:3001/sheep`
3. **部署路径：** 正式部署时作品集位于根路径 `/`，heard-sheep 必须继续保留在 `/sheep`
4. **安全：** 不得在前端加入任何 API Key、Token 或 Secret
5. **当前限制：** 不要进行 Figma、GitHub 推送和正式部署

---

## 五、明天恢复任务的第一步

1. 读取本断点文档
2. 检查 `git status` 与当前已落盘改动
3. 重新启动作品集（`npm run dev`）与 heard-sheep（`cd heard-sheep/heard-sheep && npx next dev -p 3001`）的本地预览服务
4. 启动 Chrome 调试端口：`chrome --remote-debugging-port=9222 --no-first-run --no-default-browser-check --user-data-dir=C:\temp\chrome-debug`
5. 仅聚焦解决干净截图问题：对 `input-mobile`、`candidate-tasks-mobile`、`task-detail-mobile` 运行 evaluate_script 隐藏开发标记后再截图
6. 用干净素材替换 heard-sheep 展示图片
7. 重新生成首页与 heard-sheep 详情页截图
8. 完成 TypeScript、build、占位信息与敏感信息复核
9. 人工验收通过后，再进入 Figma 最终捕获阶段

---

## 六、本轮修改文件清单

| 文件 | 状态 |
|---|---|
| `src/data/site-config.ts` | 修改 |
| `src/data/projects.ts` | 修改 |
| `src/data/navigation.ts` | 修改 |
| `src/data/demos/proddoc-ai.ts` | 新建 |
| `src/data/demos/decision-copilot.ts` | 新建 |
| `src/app/try/page.tsx` | 新建 |
| `src/app/try/proddoc-ai/page.tsx` | 新建 |
| `src/app/try/decision-copilot/page.tsx` | 新建 |
| `src/app/page.tsx` | 修改 |
| `src/app/projects/heard-sheep/page.tsx` | 修改 |
| `docs/如何手动修改作品集网站内容.md` | 修改 |
| `public/projects/heard-sheep/*.png` | 部分重截（5/9 干净） |
