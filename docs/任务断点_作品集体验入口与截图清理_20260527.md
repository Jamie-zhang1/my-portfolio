# 任务断点：作品集体验入口、截图清理与上线准备

**日期：** 2026-05-28  
**状态：** 继续执行中，已从安全功能分支恢复

---

## 当前基线

- 仓库：`https://github.com/Jamie-zhang1/my-portfolio`
- 分支：`feat/portfolio-interactive-showcase`
- 初始安全基线：`11c0eb7 feat: build editorial portfolio with interactive project showcase`
- 工作区恢复时已确认：功能分支与远端一致，工作区干净
- 已确认禁提交内容未被 Git 跟踪：`.claude-debug/`、`.claude/settings.local.json`、`.env*`、`node_modules/`、`.next/`

---

## 已完成成果

### 页面与项目结构

- [x] 首页保留当前 editorial 作品集视觉，不重做为模板站
- [x] 项目顺序保持为：
  1. `01 / 听到了咩`
  2. `02 / ProdDoc AI`
  3. `03 / AI Decision Copilot`
- [x] 已存在页面：
  - `/`
  - `/projects/heard-sheep`
  - `/projects/proddoc-ai`
  - `/projects/decision-copilot`
  - `/try`
  - `/try/proddoc-ai`
  - `/try/decision-copilot`

### 体验入口

- [x] 首页 `听到了咩 -> 立即体验`
- [x] `/try` 页 `进入听到了咩`
- [x] `/projects/heard-sheep` 页 `立即体验`
- [x] 三处入口均读取 `src/data/site-config.ts` 的 `heardSheepLiveUrl`
- [x] 默认逻辑：
  - 本地开发：`http://localhost:3001/sheep`
  - 自有服务器生产：`/sheep`
  - 临时/预览环境：可用 `NEXT_PUBLIC_HEARD_SHEEP_URL` 覆盖

### 内容核验

- [x] 已读取真实 heard-sheep 项目 `C:\Users\ZHANGJIANGMIN0902\heard-sheep\heard-sheep`
- [x] 已确认真实产品默认 `basePath=/sheep`
- [x] 已确认真实功能包括录音、音频上传、粘贴文本、图片识别、转写确认、AI 分析、候选任务、任务管理、历史记录、个人中心和 PWA 安装引导
- [x] 作品集文案保持为 Web MVP 与单用户本地 JSON 存储，不写账号体系、团队协作或云端同步等未实现能力

### 安全与历史清理

- [x] 功能分支已完成首次安全推送
- [x] 远端分支历史中不应重新引入 Claude 本地配置、调试日志、`.env`、Token 或其他真实服务凭证
- [x] `.gitignore` 保持忽略 `.claude-debug/`、`.claude/settings.local.json` 与本地环境文件

---

## 当前待完成事项

### 截图

- [ ] 对最终展示的 heard-sheep 产品截图重新生成或逐张复核
- [ ] 确认不再出现右侧粉色悬浮控件、左下角黑色开发标记、浏览器插件或调试元素
- [ ] 重新生成作品集自身截图：
  - `public/screenshots/portfolio/home-desktop.png`
  - `public/screenshots/portfolio/home-mobile.png`
  - `public/screenshots/portfolio/sheep-desktop.png`
  - `public/screenshots/portfolio/sheep-mobile.png`
  - `public/screenshots/portfolio/try-desktop.png`
  - `public/screenshots/portfolio/try-mobile.png`

### 文档与 SEO

- [x] README 已从默认 Next.js 模板改为作品集说明
- [x] 新增 `.env.example`，说明 heard-sheep 体验入口覆盖方式
- [x] 修正 `robots.txt` 的正式域名
- [x] 移除不存在的 `favicon.png` 引用
- [ ] 部署完成后补充正式线上验收结果和生产截图

### 部署

- [ ] 合并功能分支到 `main`
- [ ] 登录服务器审计 Nginx、PM2/Node 进程、DNS、80/443 和证书状态
- [ ] 将作品集部署为独立服务并保持 `/sheep` 指向原 heard-sheep 产品
- [ ] 完成 `https://heard-sheep.cloud/` 与 `https://heard-sheep.cloud/sheep` 验收

---

## 注意事项

- Vercel 预览环境的 `/sheep` 404 不等于生产同域部署错误；生产目标仍是 `https://heard-sheep.cloud/sheep`
- 不得删除 `/try`、ProdDoc AI Demo 或 AI Decision Copilot Demo
- 不得修改 heard-sheep 业务功能来适配作品集
- 不得关闭用户日常 Chrome；截图应使用 Playwright 独立无扩展上下文
- 修改服务器 Nginx 前必须备份配置，切流前后都要验证 `/sheep`

---

## 下一步

1. 完成本地 TypeScript/build 检查
2. 启动作品集与 heard-sheep production preview
3. 重新截取并复核最终展示图片
4. 阶段性提交并推送功能分支
5. 最终验收后合并 `main`
6. 审计服务器并上线根路径作品集
